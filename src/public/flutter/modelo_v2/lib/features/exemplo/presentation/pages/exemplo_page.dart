import 'package:flutter/material.dart';

class ExemploPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Exemplo')),
      body: Center(child: Text('Página de Exemplo para o Exemplo')),
    );
  }
}